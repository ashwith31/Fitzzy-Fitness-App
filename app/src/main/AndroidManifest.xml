<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnesscapstone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.VidWebViewActivity"></activity>
        <activity android:name=".Activities.ActivityEdit" />
        <activity android:name=".Activities.AdvanceActivity" />
        <activity android:name=".Activities.IntemActivity" />
        <activity android:name=".Activities.BasicPlanActivity" />
        <activity android:name=".Activities.DiseasesActivity" />
        <activity android:name=".Activities.DoctorhospitalActivity" />
        <activity android:name=".Activities.AccountActivity" />
        <activity android:name=".Activities.CalorieActivity" />
        <activity android:name=".Activities.ExerciseDActivity" />
        <activity android:name=".Activities.ExercisePActivity" />
        <activity android:name=".Activities.OtpActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>